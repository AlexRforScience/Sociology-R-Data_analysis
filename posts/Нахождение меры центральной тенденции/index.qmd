---
title: "Мера центральной тенденции"
image: photo_2024-08-07_13-29-15.jpg
toc: true
number-sections: true
crossref:
  chapters: false
execute: 
  warning: false
  message: fals
date: 08.16.2024
categories: [Статистика]
lang: ru
---

# Зачем?

Если мы собираемся проводить количественное исследование, то одним из неотъемлемых методов анализа данных является поиск меры центральной тенденции изучаемого признака. Казалось бы, что это не должно вызывать никаких трудностей у исследователей, однако (как это обычно бывает) не всё так однозначно.

# Способы нахождения

Все студенты, проходящие курс математической статистики, знакомы с самыми основными, а заодно и с наиболее часто применяемыми, методами поиска меры центральной тенденции, а именно со средним арифметическим и медианой. Если же дело доходит до анализа качественных признаков, например уровня образования, то используется мода.

Соответственно, вероятно читателю известно и то, в каких случаях уместно использовать среднее арифметическое, а в каких медиану. Для качественных признаков говорить о нормальности распределения или наличии выбросов обычно не приходится, так что все смело используют моду. В данном посте своего блога я хочу показать, как искать меру центральной тенденции переменной в R, при этом постараюсь максимально кратко расписать все нюансы и не ограничиться только средним арифметическим, медианой и модой.

## Среднее арифметическое

Среднее арифметическое, или же просто среднее (*mean*) очень популярный способ вычислить меру центральной тенденции. Среднее обозначается как [x]{style="text-decoration: overline;"} и имеет следующую формулу: $$
\bar{x} = \frac{\sum_{i}^{n} x_i}{n}
$$ Создадим переменную `x`, которая будет вектором числовых значений и найдем ее среднее арифметическое с помощью базовой функцией `mean()`:

```{r}
x <- c(4, 5, 3, 8)
mean(x)
```

Если в переменной имеются пропущенные значения, то на выход мы получим `NA`.

```{r}
x <- c(4, 5, 3, 8, NA)
mean(x)
```

Чтобы обойти данное ограничение, в коде мы должны указать `na.rm = TRUE`

```{r}
x <- c(4, 5, 3, 8, NA)
mean(x, na.rm = TRUE)
```

Во время исследования мы редко работает с отдельными переменными. Как правило, нам предстоит работать с таблицами данных. Возьмем, например, встроенные данные об автомобилях mtcars и перезапишем их в отдельный data frame, после чего посмотрим структуру:

```{r}
df <- mtcars
str(df)
```

Мы видим, что в mtcars есть несколько интервальных переменных, для примера возьмем `hp` - мощность в лошадиных силах.

Наиболее простой способ рассчитать среднее арифметическое колонки дата фрейма базовыми способами R:

```{r}
mean_hp <- mean(df$hp)
mean_hp
```

Более продвинутые методы с помощью пакетов data.table и tidyverse (а именно dplyr) позволяют рассчитать среднее по конкретным группам:

```{r}
# install.packages("tidyverse") если не установлен пакет
# install.packages("data.table") если не установлен пакет
library(tidyverse)
library(data.table)
mean_hp1 <- df %>% # с помощью tidyverse (пакет dplyr)
  group_by(vs) %>% # группируем по переменной vs
  summarise(mean = mean(hp))
mean_hp1 # в итоге получаем среднее hp для всех значений vs 

# Всё тоже самое сделаем с помощью data.table
dt <- as.data.table(df) # переводим df в data.table
mean_hp2 <- dt[, .(mean_hp = mean(hp)), by = vs] 
mean_hp2 # стоит заметить, что на выход мы получаем не tibble, а data.table
```

Интересно здесь то, что полученные значения средних `hp` для значений 0 и 1 столбца `vs` выводятся не в виде вектора с одним значением, как было в mean(df\$hp), а в формате "tibble" в случае с tidyverse, и "data.table" в случае использования пакета data.table.

::: callout-tip
# Совет

Такие методы вычисления по группам будут актуальны и для дальнейших способов оценок мер центральной тенденции.
:::

```{r}
class(mean_hp1)
class(mean_hp2)
```

Как было показано выше, найти среднее достаточно легко. Однако данный способ имеет серьезные проблемы, которые даже отразились в народном анекдоте: "Чиновники едят мясо, а я капусту, в среднем мы едим голубцы"! Иными словами, если изучаемый объект имеет выбросы, то показатель среднего будет искажен. Так если он такой неустойчивый, то есть ли смысл считать среднее, или всегда стоит отдавать предпочтение более робастным (устойчивым) способам поиска меры центральной тенденции? На самом деле это очень хороший вопрос, на практике стоит считать как среднее, так и, например, медиану. При этом если звезды сойдутся, то среднее арифметическое будет наилучшим показателем, так как в своем расчете учитывает все наблюдения, что в определенных ситуациях может быть очень важным. Далее мною будут рассмотрены еще два "средних", а именно среднее усеченное и среднее взвешенное

## Среднее усеченное

Среднее усеченное — это более робастная оценка меры центральной тенденции, так как она не учитывает наличие экстремально высоких или низких значений. Среднее усеченное отбрасывает фиксированное число сортированных значений с начала и конца последовательности значений изучаемого признака. Формула усеченного среднего: $$
\bar{x} = \frac{\sum_{i=p+1}^{n-p} x_i}{n-2p}$$

В R усеченное среднее рассчитывается следующим образом:

```{r}
x <- c(4, 5, 3, 8, 100, -80)
mean(x, trim = 0.3333) # через trim задаем процент значений, которые не учавствуются в расчете
```

Так, мы проигнорировали экстремально низкое значение -80 и экстремально высокое значение 100, из-за чего значение среднего равно `r mean(x, trim = 0.3333)`, как и было в первом примере главы про среднее арифметическое. Проблема среднего усеченного в том, что нам нужно самостоятельно указать через значение trim, сколько процентов начала и конца мы не станем учитывать в расчете меры центральной тенденции. Чаще всего это значение останавливается на 10%, то есть `trim = 0.1`, однако на реальных данных нужно смотреть на распределение переменной.

## Среднее взешенное

Среднее взвешенное требует еще больше информации для его расчета, ведь каждое значение $x_i$ умножается на его вес $w_i$. Формула среднего взвешенного выглядит так: $$\bar{x_w} = \frac{\sum_{i=1}^{n} x_iw_i}{{\sum_{i}^{n}w_i}}$$

Чтобы рассчитать среднее взвешенное в R датафрейм должен иметь колонку, в которой будут указаны веса. Создадим колонку с весами, используя синтаксис базового R, tidyverse и data.table:

```{r}
# base R
df <- mtcars
set.seed(671)
df$weights <- runif(nrow(mtcars), min = 0.1, max = 1)
str(df$weights)

# tidyverse
df <- mtcars
set.seed(671)
df <- df %>% 
  mutate(weights = runif(nrow(mtcars), min = 0.1, max = 1))
str(df$weights)

# data.table
df <- mtcars
df <- as.data.table(df)
set.seed(671)
df[, weights:= runif(.N, min = 0.1, max = 1)]
str(df$weights)
```

Как мы видим, создалась новая колонка `weights`, в котором содержатся веса для `hp`. Конечно, они не имеют никакого практического смысла, относительно данных в mtcars и созданы только для примера. Теперь рассчитаем среднее взвешенное с помощью `weighted.mean`, где сперва указывается переменная, среднее которой нужно вычислить, а после переменная, содержащая вектор весов для каждого наблюдения в переменной:

```{r}
weighted.mean(df$hp, df$weights)
```

Выбор весов определяется опытом исследователя, исходя из теоретической рамки исследования или на основе уже известной информации. Например, группы, которые редко попадают в выборку, могут получать более высокие веса, чтобы их вклад в конечные расчеты был более значимым.

## Медиана

Медиана — это значение, которое делит упорядоченный набор данных на две равные части. В отличие от среднего значения, медиана не чувствительна к выбросам — экстремальным значениям в данных. Это делает ее особенно ценным и актуальным показателем в эмпирических исследованиях, где данные часто бывают асимметричными или содержат выбросы. Например, наличие малого числа людей с очень высоким доходом могут исказить показатель среднего дохода всего населения. В этом случае медиана, которая представляет собой среднее значение, когда данные отсортированы, будет более точным показателем типичного дохода. В R медиана вычисляется следующим образом:

```{r}
x <- c(1, 1, 5, 5, 7, 100, 100000, 6)
median(x)
```

Как видно из результата, наличие выбросов не исказили значение медианы.

Помимо устойчивости к выбросам данных, медиана особенно актуальна для социологии. Так, велика вероятность, что интересующая нас переменная окажется измерена порядковой шкалой, например, от 1 до 5. Применить среднее арифметическое к такой переменной будет ошибкой, так как порядковая переменная не имеет равных интервалов, то есть разница между 1 и 2 будет иной, чем между 4 и 5. Таким образом, для порядковых переменных медиана - это один из главных инструментов поиска меры центральной тенденции, который учитывает порядковую природу данных.

## Медиана взвешенная

Если необходимо использовать веса и при этом природа данных неустойчива к выбросам и необходима робастная оценка, то используется медиана взвешенная. Математика медианы взвешенной состоит в том, что нам нужно упорядочить значения переменной, далее мы должны определить значение, в котором сумма весов равна для нижней и верхней половин сортированного списка. В R нет встроенной функции взвешенной медианы, так что придется подключить соответствующий пакет, после чего можно будет произвести вычисление для нашего датафрейма `df` с созданной колонкой `weights`:

```{r}
# install.packages("matrixStats") если не установлен пакет
library(matrixStats)
weightedMedian(df$hp, df$weights)
```

## Мода

Мода - значение признака, которое встречается максимально часто. Это хороший инструмент тогда, когда изучаемая переменная не является интервальной. Мода подходит как для бинарных переменных, так и для порядковых.

К сожалению, в R нет встроенной функции для поиска моды. Однако, вполне успешно с помощью базовых функций R моду можно рассчитать самостоятельно, для этого нужно построить частотную таблицу используя `table()` и найти максимальное значение с помощью `which.max()`:

```{r}
x <- c(0, 0, 0, 0, 1, 1, 5)
which.max(table(x))
```

Либо! Подключить специальный пакет modeest с функцией `mfv`:

```{r}
#install.packages("modeest") если не установлен пакет
library(modeest)
x <- c(0, 0, 0, 0, 1, 1, 5)
mfv(x)
```

Второй вариант, конечно, предпочтительней, так как используя два костыля из which.max и table мы получим только одну моду, если их на самом деле две:

```{r}
x <- c(0, 0, 0, 0, 1, 1, 5, 5, 5, 5)
which.max(table(x)) # на выход узнаем, что мода только одна


x <- c(0, 0, 0, 0, 1, 1, 5, 5, 5, 5)
mfv(x) # получаем правильный результат, где мода это 0 и 5
```

# Заключение

Поиск меры центральной тенденции - это неотъмлемая процедура описательной статистики исследования. На самом деле способов больше, чем я описал в данном посте, однако я постарался затронуть самые основные! Главное помнить, что перед анализом нужно внимательно изучить свои данные, то есть являются ли они непрерывными, порядковыми, бинарными или номинальными (факторами). Не будет лишним перед поиском меры центральной тенденции посмотреть на распределение интересующей переменной, построить ящик с усами. Визуализации данных я посвящу отдельный пост, или же выложу свой конспект по ggplot2 и plotly.

Так, если распределение симметрично, унимодально (имеет одну моду), не имеет выбросов, то все способы нахождения меры центральной тенденции (за исключением методов с весами) дадут примерно одно и то же значение. Однако, если всё плохо, что чаще всего и происходит, то лучше воспользоваться робастными оценками, такими как медиана или усеченное среднее.

Это очень важный момент, который необходимо усвоить для дальнейшего анализа данных, такого как сравнение средних (t-тест и дисперсионный анализ).
